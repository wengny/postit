<%= render 'shared/content_title', title: "Create a new Category" %>

<%= render 'shared/errors.html.erb', obj: @category %>


<div class='well'>
  <%= form_for @category do |f| %>
    <div class='control-group'>
        <%= f.label :name %>
        <%= f.text_field :name %>
    </div>
    <%= f.submit 'Create Category', class: 'btn btn-primary' %>
  <% end %>
</div>