<% if obj.errors.any? %>
  <div class='alert alert-error'>
  <h5> <%= pluralize(obj.errors.count, "error")%>
      prohibited it to be saved.
  </h5>
    <ul>
      <% obj.errors.full_messages.each do |msg| %>
        <li><%= msg %> </li>
      <% end %>
    </ul>
  </div>
<% end %>